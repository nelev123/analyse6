<?xml version="1.0" encoding="UTF-8"?>

<section>
    <title>De onbepaalde integraal</title>
    <p>
      Een bepaalde integraal kan berekend worden van zodra we een primitieve functie kennen:
      <me>
        \int_a^b f(x) \; dx = F(b) - F(a)
      </me>
      Wat het berekenen van integralen betreft gaan we ons vanaf nu hoofdzakelijk beperken tot het berekenen van primitieve functies.
    </p>
  
    <example>
      <p>
        Een primitieve functie van <m>x</m> is gelijk aan <m>\dfrac{x^2}{2}</m>, want <m>D\left ( \dfrac{x^2}{2} \right)=x</m>. De functies <m>\dfrac{x^2}{2}+1</m>, <m>\dfrac{x^2}{2}+20</m> en  <m>\dfrac{x^2}{2}-1000</m> zijn echter ook allemaal primitieve functies van <m>x</m>. Dit leidt ons tot de onderstaande definitie.
      </p>
    </example>
  
      <assemblage>
          <title>Onbepaalde integraal</title>
          <p>
          De onbepaalde integraal is de verzameling van alle primitieve functies van <m>f(x)</m>. We noteren
          <me>
              \int f(x) \; dx = F(x) + c
          </me>
          met <m>c</m> een willekeurig reëel getal.
          </p>
      </assemblage>
  
      <example>
          <p>
          <m>\int (-x^3+2x-5) \; dx = -\dfrac{x^4}{4}+x^2-5x + c </m>
          </p>
      </example>
  
      <p>
          Uit onderstaande basisformules voor het afleiden, volgt telkens de corresponderende onbepaalde integraal.
      </p>
  
      <assemblage xml:id="assemblage-onbepaaldeint">
       <title>Onbepaalde integralen</title>
       <p>
        <md>
          <mrow> \amp D(x^r) = r x^{r-1} \quad (r \in \mathbb{R}) \amp \amp \int x^r \; dx =\frac{x^{r+1}}{r+1} + c  \quad ( r \neq -1)</mrow>
          <mrow> \amp D(\sin x) = \cos x \amp \amp \int \cos x \; dx =  \sin x + c </mrow>
          <mrow> \amp D(\cos x) = - \sin x \amp \amp \int \sin x \; dx = - \cos x + c </mrow>
          <mrow> \amp D(\tan x) = \frac{1}{\cos^2 x} \amp \amp \int \frac{1}{\cos^2 x}  \; dx =  \tan x + c </mrow>
          <mrow> \amp D(\cot x) = - \frac{1}{\sin^2 x} \amp \amp \int \frac{1}{\sin^2 x}  \; dx =  -\cot x + c </mrow>
          <mrow>\amp D(\ln x) = \frac{1}{x} \amp \amp  \int \frac{1}{x} \, dx = \ln |x| +C </mrow>
          <mrow>\amp D(e^x) = e^x \amp \amp  \int e^x  \, dx = e^x + C </mrow>
          <mrow>\amp D(a^x) = a^x \cdot \ln a  \amp  \amp \int a^x  \, dx = \frac{a^x}{\ln a} + C</mrow>
        </md>  
      </p>
      </assemblage>

      <remark>
        <p>
          De functie <m>\dfrac{1}{x}</m> is de afgeleide van <m>\ln x</m> en <m>\ln (-x)</m>:
          <md>
            <mrow> \amp x \in \mathbb{R}^+_0 \Rightarrow D(\ln x)=\frac{1}{x}</mrow>
            <mrow> \amp  x \in \mathbb{R}^-_0 \Rightarrow D(\ln(-x))=\frac{1}{-x} \cdot (-1)
              =\frac{1}{x} </mrow>
          </md>
          De onbepaalde integraal <m>\displaystyle \int \frac{1}{x} \, dx</m> is dus gelijk aan
          <m>\ln |x| +C</m>.
        </p>
      </remark>
  </section>